# AI 任务提交与成员能力画像智能体 · 介绍

> 对应《爱管理_三大AI智能体功能设计.md》第 4 章(功能定位/数据输入/工作分析/难度分析/动态画像/团队风险/统一审核)。
> 本文档随实现进度更新;当前已覆盖 4.1–4.12 全部可落地项(缺数据源的项如实说明,不臆造)。

## 更新日志
### v2.4 (2026-09-15)

- **配置手册补全**:说明文档第三章扩为 LLM 接入完整步骤(DeepSeek 密钥申请→setx→重启→验证 engine=llm→排错表);第八章 8.1 扩为 GitHub 从零到跑通(创建 PAT→环境变量→生产库迁移→状态验证→触发同步→确认进画像→排错表)

## 更新日志
### v2.3 (2026-09-15)

- **GitHub 接入(US34)**:commit/PR 合并/PR Review/Issue 创建四类真实事件可配置同步(github.* 配置块 + GITHUB_* 环境变量),github_event_id 唯一索引幂等去重;成员映射(配置映射→登录名→邮箱本地部分→提交者名),未匹配成员的活动不写库(记 warnings,不造假)
- 工具表新增第 8 个工具 query_git_commits:LLM 发现7 天无活动时先查 GitHub 真实活动核实,再下停滞结论(验收标准 2 落地)
- 新端点:GET /github/status(全员)、POST /github/sync(admin/owner 手动同步);前端画像面板加GitHub 状态 + 同步按钮`r
- 自动同步:设置 GITHUB_AUTO_SYNC_HOURS 后每小时回溯同步;生产库执行 docs/github_migration.sql 一次补列
- 全量测试回归通过(基线 137 + GitHub 接入新用例)

## 更新日志
### v2.2 (2026-09-15)

- **对齐设计文档 4.1–4.12 差距修复**(详见《任务提交与画像智能体_说明.md》第十章对照表)
- 4.2/4.6/4.8:新增 `test` 活动类型,支持测试活动记录;成员级与团队级均新增"缺测试活动"提示
- 4.5:难度评估新增"影响后续任务"因子(统计被后续任务依赖数,规则引擎 +5/+10,LLM facts 注入)
- 4.3/4.4/4.6:成员客观事实新增跨模块统计、进行中近 7 天无活动、高难度缺 Review、多人共改同一任务
- 4.7:画像新增 代码稳定性/返工率/缺陷修复能力/Review 参与度 四字段(LLM 与规则引擎双路输出);快照改为存完整分析证据(客观事实+推断+画像+evidence_refs),可回溯到任务与代码证据
- 4.8:团队风险新增 missing_test、module_concentration(模块工作集中,单人占比≥60% 触发)
- 4.9:周期对比新增"测试活动"指标
- 前端画像面板展示新增四字段;全部改动已通过编译与 137 项契约测试回归

## 更新日志

### v1.0 (2026-09-13)

- **首版发布**:AI 任务提交与成员能力画像智能体完整落地(分支 `feat/profile-agent`)
- 成员工作摘要(4.3):事实层 + AI 推断层,带证据、不编造
- 成员贡献分析(4.4):Review 参与/缺陷修复/返工提示/按时交付/协作贡献(参与他人任务 Review)
- 任务难度自动评估(4.5):依据逐条可追溯,人工修正永久优先
- 提交异常模式(4.6):未关联任务/同标题重复提交/信息不清晰/完成无代码活动,证据化描述
- 动态能力画像(4.7):快照 + 趋势变化原因 + 成员纠正机制
- 团队风险(4.8):负载过高/单点依赖/任务停滞/高难度缺 Review/里程碑延期,共 5 类
- 时间范围(4.9):快捷周期(本周/本月/近 30 天)+ 本期 vs 上期对比分析
- 贡献绿格子热力图(4.10):按日聚合,点击查看当天活动
- 统一审核中心接入(第 5 章):风险建议一键送审,人工采纳后落需求池,全程留痕
- 修复:里程碑风险误报(项目相对周换算)、批量导入事务性、送审去重

## 这是什么

画像智能体回答三个问题:**每个人在一段时间内做了什么、承担了多大难度的工作、项目当前有什么风险**。
它不统计提交次数,而是基于活动事实、任务状态、难度评估与成员容量做证据化分析——每条结论都带依据,数据不足时如实说明,不编造。

## 核心能力(与设计文档的对应)

| 能力 | 文档章节 | 实现 |
|---|---|---|
| 成员工作摘要 | 4.3 | 按时间范围输出每人的活动明细(事实层)+ 归纳推断(带依据),未关联任务的活动单独提示 |
| 成员贡献分析 | 4.4 | Review 参与度、缺陷修复数、返工提示(同任务多次 bugfix)、按时交付(项目相对周换算)、预计工时口径说明、**协作贡献**(参与他人任务 Review 的次数与对象) |
| 任务难度分析 | 4.5 | 按工时/依赖数/核心模块/阻塞/跨周自动评分定级(low/medium/high/extreme),依据逐条可追溯;人工修正永久优先 |
| 提交异常模式 | 4.6 | 未关联任务、同标题重复提交、提交信息不清晰(标题过短/占位词)、任务完成但无代码活动、返工、延期;输出证据化描述而非评判个人 |
| 动态能力画像 | 4.7 | 擅长方向/难度承受/交付及时性/负载/推荐任务类型,每次分析落快照;**快照趋势**:相邻快照对比给出画像变化原因;**成员纠正机制**:成员可对 AI 画像(擅长方向等)提交纠正,永久保留并展示在画像旁(本人或 admin/owner) |
| 团队风险与协调建议 | 4.8 | 负载过高(≥90%)/关键单点依赖(≥2 项高难度)/任务长期无进展(7 天无活动)/**高难度任务缺 Review**/**临近或超过计划截止周未完成(里程碑风险)**,每条带证据+推断+建议行动 |
| 时间范围查询与对比 | 4.9 | 快捷周期(本周/本月/近 30 天)+任意起止日期;本期 vs 上期对比(活动量/Review/缺陷修复/完成任务/完成率/预计工时/未关联活动/高难度承担),实际完成工时缺数据源时如实说明 |
| 贡献活动可视化 | 4.10 | **贡献绿格子热力图**(按日聚合 commit/pr/review/bugfix/task_done/note,点击某天查看当天活动);明确标注「绿格子=活动分布,不等于工作质量」 |
| 数据输入 | 4.2 | 活动事实支持**批量导入**(单次 ≤200 条,source=import);实际工时/测试覆盖率/真实 GitHub 事件暂无数据源,文档已说明数据不足口径 |
| 统一审核中心接入 | 第 5 章 | 团队风险可一键转成待审建议(kind=profile),由 admin/owner 采纳/拒绝;采纳后写入需求池,全程留痕 |

## 使用入口

前端 **AI 助手 → 画像智能体** 面板:选时间范围 → 查询(预览)/触发正式分析(留痕)→ 切成员看摘要/画像/异常模式 → 绿格子热力图点击看当天 → 展开难度分析 → 展开「本期 vs 上期」对比 → 风险建议送审(去「AI 审核中心」处理)。

## API 一览

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| POST | `/api/profile-agent/activities` | 录入活动事实(commit/pr/review/bugfix/task_done/note) | 登录成员 |
| POST | `/api/profile-agent/activities/import` | 批量导入活动事实(单次 ≤200 条) | 登录成员 |
| GET | `/api/profile-agent/activities?userId=&start=&end=` | 活动列表 | 任何登录用户 |
| GET | `/api/profile-agent/heatmap?start=&end=&userId=` | 贡献绿格子热力图(按日聚合) | 任何登录用户 |
| GET | `/api/profile-agent/difficulty` | 全量任务难度(AI 评估+人工覆盖优先) | 任何登录用户 |
| PATCH | `/api/profile-agent/difficulty/{taskId}` | 人工修正难度(admin/owner) | admin/owner |
| GET | `/api/profile-agent/analysis?start=&end=` | 整包分析预览(不留痕) | 任何登录用户 |
| GET | `/api/profile-agent/analysis/compare?prevStart=&prevEnd=&start=&end=` | 本期 vs 上期对比分析(4.9) | 任何登录用户 |
| POST | `/api/profile-agent/analysis/run?start=&end=` | 正式分析(留痕,失败可重试) | 任何登录用户 |
| GET | `/api/profile-agent/runs` | 分析运行记录(最近 20 条) | 任何登录用户 |
| POST | `/api/profile-agent/risks/submit-suggestions?start=&end=` | 团队风险 → 送审 | 登录成员 |
| GET | `/api/profile-agent/snapshots?userId=` | 画像快照历史 | 任何登录用户 |
| GET | `/api/profile-agent/snapshots/trend?userId=` | 画像快照趋势(变化原因对比,4.7) | 任何登录用户 |
| POST | `/api/profile-agent/corrections?userId=&field=&correctedValue=&reason=` | 提交画像纠正(本人或 admin/owner,4.7) | 登录成员(本人) |
| GET | `/api/profile-agent/corrections?userId=` | 画像纠正历史(最新在前) | 任何登录用户 |

## 数据表(均幂等建表,见 `db/schema.sql`)

- `activity_records` — 活动事实(手动/导入);task_id 可空,空即异常模式证据
- `difficulty_assessments` — 难度评估;`assessed_by=manual` 永远优先于 ai
- `profile_snapshots` — 画像快照(按时间范围,趋势对比数据源)
- `profile_agent_runs` — 分析运行留痕(成功/失败均可重试)
- `profile_corrections` — 成员画像纠正(文档 4.7 纠正机制,永久保留)
- 另:`meeting_suggestion_records.meeting_id` 改为可空,使非会议来源的建议(画像智能体)能进入统一审核流

## 边界与原则

- 客观事实与 AI 推断分层输出,推断必须带依据;
- 提交次数 ≠ 工作量 ≠ 工作质量;绿格子 ≠ 能力评分;
- AI 只建议,任务重新分配等正式变更由人工在审核中心确认后执行;
- 暂无数据时明确提示,不臆测(如实际完成工时、测试覆盖率、真实 GitHub 事件);
- 难度/画像均可由人工修正,人工结论永久优先于 AI 推断。
