# AI 任务提交与成员能力画像智能体 · 说明文档

> 版本:v2.3(LLM 内核版+治理修复+GitHub 接入) · 分支 `feat/profile-agent`(仅本地,未推送)
> 对应《爱管理_三大AI智能体功能设计.md》第 4 章与《需求基线与 AI 智能体需求交接 v2.0》US34–US37

> v2.1 修复(2026-09-15):活动录入越权(仅本人/admin-owner 代录)、GET 预览零副作用零扣费
> (不再调 LLM/不再写库)、任务级评估锁防并发重复、ThreadLocal 录制泄漏收尾、LLM 幻觉实体过滤、
> 快照统一由系统落库(移除 write_snapshot 工具)、规则引擎降级独立标注 assessed_by=rules、
> 时间范围上限 400 天、正式分析限流(30s/用户)、attempt 递增、人工修正附理由等。
> 另补每周定时分析开关(默认关闭,见「自主触发」)。

---

## 一、这个智能体是什么

回答三个问题:**每个人在一段时间内做了什么、承担了多大难度的工作、项目当前有什么风险**。

v2.0 起,它是一个**真正的智能体**,不再是规则引擎:

| 部件 | v1.0(规则引擎) | v2.0(LLM 内核,当前) |
|---|---|---|
| 推理 | if-else 打分卡 + 字符串模板 | **DeepSeek LLM 生成结论**,规则引擎仅作降级备胎 |
| 规划/决策循环 | `analyze()` 一把梭 | **感知→分析→发现缺口→调用工具→再分析→收敛**,步数受控 |
| 工具调用 | 无,只读本地表 | **7 个查询工具**(任务/活动/成员/画像/难度历史/快照/纠正),模型自主选择;快照写入是系统权威操作,由正式分析统一落库 |
| 记忆 | 快照只是存档 | **长期记忆检索**:历史画像轨迹 + 成员纠正 + **人工修正先验回灌** |
| 自主补数据 | 数据不足只说"暂无" | 发现疑点(如 7 天无活动)**主动调工具核实**再下结论 |
| 可观测 | 无 | 每步 **thought/action/observation/tokens/model** 全留痕,结论锚定 evidence_refs |

## 二、架构

```
ProfileAgentService(REST 门面,文档 4 章全部功能)
        │
ProfileAgentBrain(大脑,三个核心方法)
   ├─ llm.assessDifficulty(task)      难度评估 → level/score/basis/confidence/evidence_refs
   ├─ llm.analyzeMember(facts,memory) 成员画像 → ai_inference/dynamic_profile/confidence
   └─ llm.analyzeTeamRisks(facts)     团队风险 → risks[](带证据与建议行动)
        │ 全部走 ↓
ProfileAgentRuntime(规划循环)
   感知(prompt 注入事实+记忆) → LLM 返回 {thought, action:{tool,args}} 或 {final}
   → action: ProfileToolRegistry 执行,observation 回填上下文,继续下一轮
   → final: 结构化 JSON 结论
   → 步数耗尽强制收敛;每步留痕 Step(thought/action/observation/tokens/model)
        │
ProfileToolRegistry(工具注册表,7 个)
   query_tasks / query_activities / query_members / query_member_profile
   query_difficulty_history / query_profile_snapshots / query_corrections
   (v2.1 起移除 write_snapshot:快照写入统一由 runAnalysis 系统落库,防时间轴污染)
        │
ProfileMemoryStore(记忆层)
   难度记忆:人工修正先验(反馈回灌)+ 同任务历史评估
   成员记忆:快照轨迹 + 成员纠正记录(优先采信)
        │
ProfileLlmClient(独立 DeepSeek 客户端)
   JDK HttpClient / OpenAI 兼容协议 / jsonMode / token 统计
```

**独立性**:以上组件全部位于 `com.aicap.profile` 包,配置前缀 `profile-llm.*`、环境变量 `PROFILE_LLM_*`,与会议智能体(`com.aicap.agent`、`llm.*`、`AICAP_LLM_*`)**完全隔离**,互不影响。

## 三、配置(密钥自己管)

| 环境变量 | 说明 | 默认 |
|---|---|---|
| `PROFILE_LLM_API_KEY` | 你的 DeepSeek 密钥(**必填**,不配则自动降级规则引擎) | 空 |
| `PROFILE_LLM_BASE_URL` | API 地址 | `https://api.deepseek.com` |
| `PROFILE_LLM_MODEL` | 模型名 | `deepseek-chat` |
| `PROFILE_LLM_MAX_STEPS` | 规划循环最大步数(防失控) | 8 |

设置方式(PowerShell,只设一次,持久生效):

```powershell
setx PROFILE_LLM_API_KEY "sk-你的DeepSeek密钥"
```

设完重启后端生效。**降级安全**:密钥未配置或 LLM 调用失败/输出不合法时,自动回落到 v1.0 规则引擎,接口永不因 LLM 故障而不可用;响应里 `engine`/`generated_by` 字段明确标注本次结论来自 `llm` 还是 `rules_fallback`。v2.1 起规则引擎降级结论落库时 `assessed_by=rules`(与 LLM 的 `ai` 区分,避免规则打分污染 LLM 历史记忆)。

## 三·五、自主触发(定时分析,默认关闭)

```yaml
profile-agent:
  scheduled:
    enabled: ${PROFILE_AGENT_SCHEDULED_ENABLED:false}   # true 开启
    cron: ${PROFILE_AGENT_SCHEDULED_CRON:0 0 9 * * MON} # 每周一 09:00
```

开启后每周一自动对最近 7 天做一次正式分析(触发者取库中第一个 admin/owner;无则跳过并告警)。
默认关闭:未配置 LLM 密钥时跑的是规则引擎,且正式分析会产生 API 费用,故不静默开启。
事件触发(任务完成/PR 合并/连续 N 天无活动)与主动澄清(askMember)依赖外部数据源
(GitHub 同步 US34)与 IM 通道,当前不可用,工具表已预留扩展位。

## 四、防幻觉与可解释(治理红线)

- **每条结论必须带 `confidence`(0–1)与 `evidence_refs`**(锚定到具体工具调用/事实编号),存库可回溯;
- LLM 输出经**结构校验**(level 合法、score 0–100、basis 非空;风险条目必须有 kind+evidence,不合规的条目直接丢弃);
- **红线注入 prompt**:不得把提交次数等同于工作量/质量、不得输出对人的评判、与成员纠正冲突时以纠正为准、新成员数据不足时建议"先观察";
- 全部留痕:正式分析的每一步(thought/调了什么工具/看到了什么/花了多少 token)写入 `profile_agent_runs.result_json.observability`,事后可完整回放"为什么得出这个结论"。

## 五、人工反馈闭环(验收标准 5)

1. AI 评估任务难度 → 落库(`assessed_by=ai`);
2. 项目负责人人工修正(`assessed_by=manual`,**永远优先**,AI 不覆盖);
3. 下次评估同类任务时,`ProfileMemoryStore.difficultyMemory` 把所有人工修正作为**"必须尊重的先验"注入 prompt**;
4. LLM 在人工先验的基础上推理,而不是重复犯错 → 评估随使用越来越准。

成员侧同理:成员对画像的纠正(`profile_corrections`)在每次成员分析时注入并**优先采信**。

## 六、API 一览(全部保留兼容)

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/profile-agent/analysis?start=&end=` | 整包分析预览(纯只读:复用已存评估,缺失任务用规则引擎即时算;不调 LLM、不写库、不留痕) |
| POST | `/api/profile-agent/analysis/run?start=&end=` | 正式分析:留痕+落快照,**result_json 含完整 steps 回放** |
| GET | `/api/profile-agent/difficulty` | 任务难度(LLM 评估+人工修正优先) |
| PATCH | `/api/profile-agent/difficulty/{taskId}` | 人工修正难度(admin/owner) |
| POST/GET | `/api/profile-agent/activities`(+`/import`) | 活动事实录入/批量导入/查询 |
| GET | `/api/profile-agent/heatmap` | 贡献绿格子热力图(按日聚合) |
| GET | `/api/profile-agent/analysis/compare` | 本期 vs 上期对比 |
| GET | `/api/profile-agent/snapshots`(+`/trend`) | 画像快照/趋势 |
| POST/GET | `/api/profile-agent/corrections` | 成员纠正提交/历史 |
| POST | `/api/profile-agent/risks/submit-suggestions` | 团队风险→统一审核中心(kind=profile) |
| GET | `/api/profile-agent/runs` | 运行记录(最近 20 条) |

## 七、验收标准对照

| 标准 | 状态 | 实现位置 |
|---|---|---|
| 1 反事实测试(不同任务给不同且有据的评估) | ✅ 机制就位 | 无关键字匹配;LLM 按任务事实+记忆推理;`basis` 逐条不同 |
| 2 主动补数据(7 天无活动→先查 Git 再报警) | ✅ 循环支持 | 步进协议明示"发现可疑点先调工具核实";query_activities 可跨周期查证 |
| 3 可解释(结论可追溯 tool_call+evidence+prompt) | ✅ 已实现 | evidence_refs + observability.steps 落库回放 |
| 4 自适应(新成员→建议先观察) | ✅ 红线注入 | 记忆为空时 prompt 明示"不要硬下结论,建议先观察" |
| 5 闭环改进(人工修正回写后下次利用) | ✅ 已实现 | difficultyMemory 人工先验注入(见第五节) |

> 注:标准 2 的"查 Git"自 v2.3 起可用——GitHub 同步(US34)已接入,工具表注册了第 8 个工具 `query_git_commits`(查 `source=github` 的真实活动),循环无需改动。

## 八、当前数据源边界(如实说明)

- 活动事实三来源:手动录入/批量导入(`source=manual/import`)+ **GitHub 同步(v2.3 起,`source=github`)**,见下节"GitHub 接入";
- 实际完成工时/测试覆盖率暂无数据源,结论中如实标注;
- 未读代码内容、不做代码质量静态扫描——质量信号当前为行为模式(返工/缺 Review/状态不一致),详见介绍文档。

### 8.1 GitHub 接入(US34,可配置)

配置(环境变量,`application.yml` 的 `github.*` 块,重启生效):

| 环境变量 | 含义 | 默认 |
|---|---|---|
| `GITHUB_ENABLED` | 总开关 | `false`(未配置时不发任何外部请求,接口返回"未配置") |
| `GITHUB_TOKEN` | PAT(只需 repo 只读权限,勿提交) | 空 |
| `GITHUB_REPO` | 仓库 `owner/repo` | 空 |
| `GITHUB_API_BASE` | API 地址 | `https://api.github.com` |
| `GITHUB_USER_MAPPING` | JSON:GitHub 登录名→系统 username,如 `{"li-ming":"李锐铭"}` | 空 |
| `GITHUB_AUTO_SYNC_HOURS` | 定时自动同步回溯窗口(小时),0=不自动 | `0` |

同步的事件(时间范围内):commit(sha 幂等)、PR 合并、PR Review、Issue 创建;`github_event_id` 唯一索引保证重复同步不产生重复活动。成员映射优先级:配置映射 → GitHub 登录名=系统 username/displayName → 作者邮箱本地部分 → 提交者名;未匹配到系统成员的活动**不写库**并记入 warnings(不造假)。commit/PR 文本含 `Txx` 且任务存在时自动关联任务;commit 消息按 Angular `(scope):` 提取模块。

接口:`GET /api/profile-agent/github/status`(全员)、`POST /api/profile-agent/github/sync?start=&end=`(admin/owner,手动同步)。前端画像面板工具栏已加"GitHub 状态 + 同步按钮"。

生产库执行一次:`mysql -u aiguanli -p AIcap < docs/github_migration.sql`(补 `github_event_id` 列与唯一索引);测试库由 schema.sql 自动重建。

## 九、文件清单(全部新增,均在 `com/aicap/profile`)

| 文件 | 职责 |
|---|---|
| `ProfileLlmProperties` | 独立配置(`profile-llm.*`) |
| `ProfileLlmClient` | DeepSeek HTTP 客户端(独立栈,token 统计,jsonMode) |
| `ProfileToolRegistry` | 8 个查询工具(含 `query_git_commits`)+ schema 注出 + 安全执行(不含写快照) |
| `ProfileAgentRuntime` | 规划循环(步进协议/步数控制/强制收敛/Step 留痕) |
| `ProfileMemoryStore` | 记忆层(快照轨迹/人工修正先验回灌/成员纠正) |
| `ProfileAgentBrain` | 大脑:三核心方法 LLM 化 + 步骤录制 + 结构校验 + 降级信号 |
| `ProfileScheduledAnalyzer` | 每周定时分析(默认关闭,配置 `profile-agent.scheduled.enabled`) |
| `GitHubProperties` | GitHub 同步配置(`github.*`,US34) |
| `GitHubActivitySyncService` | GitHub 事件同步:commit/PR 合并/Review/Issue 创建,成员映射,幂等,定时自动同步 |

改造保留:`ProfileAgentService`/`Controller`、`ProfileAgentRun`、`ProfileSnapshot`、审核中心接入、权限与失败落库——治理骨架不动,大脑换芯。

---

*v2.0 · 2026-09-15 · 仅本地分支,未经推送*


---

## 十、与《爱管理_三大AI智能体功能设计》第 4 章逐条对照(2026-09-15 v2.2)

> 对照对象:设计文档 4.1–4.12。每条列:文档要求 → 当前实现 → 状态(✅已实现 / 🛠本轮补齐 / ⚠️无数据源,如实说明)。

### 4.1 功能定位
- ✅ 成员工作事实 / 任务难度 / 成员贡献 / 动态画像 / 团队状态 / 项目风险与协调建议——`/analysis` 整包覆盖。

### 4.2 数据输入
| 文档数据 | 现状 | 状态 |
|---|---|---|
| GitHub:Commit/Push/PR/Issue/Branch/Review 意见 | 🛠 v2.3 接入:commit/PR 合并/Review/Issue 创建可配同步(`source=github`,US34);Push 合并入 commit 集,Branch 无独立事件,Review 意见取 state+首行正文 | 🛠 已接入 |
| 任务:负责人/状态/Sprint/截止/预计工时/依赖/难度/阻塞 | 负责人/状态/预计工时/依赖/难度/阻塞均有;Sprint 无实体(用项目相对周) | ✅/⚠️ |
| 任务:实际工时/优先级/所属模块/验收结果 | tasks 表无这些字段,无数据源 | ⚠️ |
| 质量:测试用例/覆盖率/缺陷严重程度/修复时间/返工次数 | 缺陷修复次数(活动计数)有;测试维度🛠本轮新增 `test` 活动类型 | 🛠 部分 |
| 协作:Review 通过情况/交接/会议行动项完成情况 | 无数据源 | ⚠️ |
| 成员:技术方向/技能/负载/Bandwidth/历史延期返工 | tech_stack/负载/带宽有;历史延期返工无历史表 | ⚠️ 部分 |

### 4.3 成员实际工作内容分析
- ✅ 活动明细/未关联任务/主模块推断/数据不足说明;
- 🛠 本轮补:王五场景"进行中任务近 7 天无活动"成员级提示(`long_inactive_tasks`)。

### 4.4 成员贡献分析
- ✅ 完成任务数/难度承担/复杂依赖/修改模块/Review 次数/缺陷数/负载;
- 🛠 本轮补:跨模块任务统计(`cross_module_modules/cross_module_count`,≥3 模块时给提示);
- ⚠️ 未实现:实际完成时间(按时/延期按 week_end+活动推断)、Review 通过情况、缺陷严重程度、返工率趋势、交接/行动项——无数据源。

### 4.5 任务难度分析
- ✅ 预计工时/依赖数/核心模块/权限安全/阻塞 因子 + low/medium/high/extreme + 依据;
- 🛠 本轮补:**"影响后续任务"因子**——统计每个任务被多少后续任务依赖,规则引擎 +5/+10,LLM facts 注入 `downstream_tasks`(设计文档示例"被两个后续任务依赖→高难度"已落地);
- ⚠️ 无数据源:实际完成时间、公共接口、复杂边界、多人协作因子(LLM 可基于事实综合,规则引擎不硬编码)。

### 4.6 提交质量和异常模式
- ✅ 相似重复提交/信息不清/未关联任务/同一问题反复修改/长期无进展/状态与活动不一致(完成无活动);
- 🛠 本轮补:成员级 Review 缺失(`missing_review_tasks`)、测试缺失(`missing_test_tasks`)、多人重复修改同一问题(`multi_member_edits`);
- ⚠️ 无合入状态字段,"代码已合入但任务未完成"无法识别。

### 4.7 动态成员能力画像
- ✅ 擅长方向/难度承受/交付及时性/负载/风险/推荐任务类型 + 客观事实与 AI 推断分离 + 成员纠正优先;
- 🛠 本轮补画像字段:`code_stability`(代码稳定性)、`rework_rate`(返工率)、`defect_fix_capability`(缺陷修复能力)、`review_participation`(Review 参与度)——LLM prompt 与规则引擎降级双路输出,前端已展示;
- 🛠 本轮修:**快照证据可回溯**——`saveSnapshot` 从"只存 dynamic_profile"改为存完整成员分析(objective+ai_inference+dynamic_profile+evidence_refs+confidence),`dynamic_profile` 字段同时提升顶层兼容旧快照读取;趋势/快照接口无需改;
- ⚠️ Sprint 对比无实体;能力轨迹(成长/停滞)需多周期快照,接口已支持按时间查。

### 4.8 团队贡献和项目风险
- ✅ 负载过高/关键单点/长期无进展/高难度缺 Review/里程碑延期;
- 🛠 本轮补:`missing_test`(哪些任务缺测试活动)、`module_concentration`(模块工作集中在少数人,单人占比≥60% 且活动≥3 条触发);送审 kind→中文映射同步扩展;
- ⚠️ 能力空缺/多人重复工作:无技能-任务匹配数据源,module_concentration 部分覆盖。

### 4.9 时间范围和对比
- ✅ 自定义/本周/本月/近30天 + 任意两周期对比(活动量/Review/缺陷/完成率/负载);
- 🛠 本轮补:对比指标加"测试活动"(`test_count`);
- ⚠️ 当前/上一 Sprint、版本/里程碑周期:数据模型无 Sprint/版本实体,未做。

### 4.10 贡献活动可视化
- ✅ 绿格子热力图(commit/pr/review/bugfix/task_done/note 六类,点击看当日明细,已自动纳入新增 `test` 类);
- ✅ 成员摘要/难度分布/负载/风险展示;✅ 真实 GitHub 活动流(v2.3 接入,配置后 `source=github` 自动进入分析)。

### 4.11 输出结果
- ✅ 成员摘要/贡献/难度/画像/团队报告/异常分析/负载/风险/协调建议;
- ⚠️ 代码质量静态分析、测试覆盖率、Review 通过率、返工延期趋势图:无数据源,如实标注。

### 4.12 与其他智能体关系
- ⚠️ 给会议智能体/规划画布提供画像依据:数据 API 已具备(`/analysis`/`/snapshots`),跨智能体对接层未实现;画像风险已通过 `kind=profile` 送统一审核中心。

---

*v2.3 · 2026-09-15 · 仅本地分支,未经推送*