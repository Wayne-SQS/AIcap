# 爱管理 · 缺陷清单与修复建议

> 由测试执行发现;严重级:S=严重/M=中等/L=轻微。
> 定位:前端 `index.html`(1 处)、后端 `backend/app`(3 处)。
> **状态:4 项缺陷均已由开发修复,测试已独立复核通过(复核证据见文末「修复复核记录」)。**
>
> **2026-09-07 全面审查追加:新增 4 项缺陷(BUG-FE-02/03、BUG-BE-04、BUG-QA-01,其中前 3 项已修复复核通过、BUG-QA-01 待修)与 7 项待完善观察项,详见后文与「修复复核记录(2026-09-07)」。**

---

## BUG-FE-01 【M·前端】页面刷新后登录态状态栏失真,logout/user-chip 隐藏

- **现象**:已登录用户(如 成员1)刷新页面后,右上角状态芯片停留在「在线 · 未登录」,user-chip 与「退出」按钮不可见;但看板数据正常加载(证明 token 有效、/api/auth/me 成功)。
- **E2E 证据**(`qa/ui-e2e.spec.js` OE-12):
  ```
  OE-12 刷新后状态栏仍显示已连接(期望;已知缺陷)  ✘
  chip="在线 · 未登录" userChipVisible=false
  ```
- **复现步骤**:后端可达 → 登录成员1 → 按 F5/刷新 → 观察 mode-chip。
- **根因分析**:`index.html` `bootstrap()` 中(约 L1372-1377):
  ```js
  if(apiMode){
    updateChip();                    // 此时 currentUser 仍为 null → 显示"未登录"
    if(authToken){
      try{currentUser=await api('/api/auth/me');await loadAll();renderAll();}
      ...
  ```
  `/me` 成功赋值 `currentUser` 后**没有再次调用 `updateChip()`**,因此 UI 停留在调用前渲染的「在线 · 未登录」状态。
- **修复建议**:
  ```js
  try{currentUser=await api('/api/auth/me'); updateChip(); await loadAll();renderAll();}
  ```
  并补一条回归用例:登录→刷新→断言 `#mode-chip` 文本含「在线 · 已连接后端」且 `#logout-btn` 可见。
- **影响**:用户刷新后误以为自己掉线/无法退出;对权限演示(AI 审核、拖拽写操作)仍正常,但体验与状态一致性受损。

---

## BUG-BE-01 【M·后端】stories 缺状态/迭代取值域校验 → 脏数据使 dashboard 口径不自洽

- **现象**:`POST/PATCH /api/stories` 接受 `status:5`、`sprint:99` 等越界值并入库;仪表盘按 0/1/2 分桶统计,于是 `total=21` 而 `todo+doing+done=20`、`by_sprint` 合计=21,与“total=各状态之和”的验收口径矛盾。
- **复现**(QA 库 8001,实测):
  ```
  基线: total=20 桶合计=20 by_sprint合计=20
  创建 {status:5}: total=21 桶合计=20 by_sprint合计=21   ← 不自洽
  删除后回 20;cleanup ok
  ```
- **根因**:`schemas.StoryIn/StoryPatch` 的 `status/sprint/activity` 无 `ge/le` 或枚举约束;`models.Story` 也未声明 CHECK。
- **修复建议**:
  - `status: int = Field(default=0, ge=0, le=2)`
  - `sprint: int = Field(default=1, ge=1, le=3)`
  - `activity: int = Field(default=2, ge=1, le=5)`
  - 旧脏数据需清洗或由 dashboards 兜底(推荐拒绝写入,前端已限死合法值)。
- **回归用例**:扩展 pytest 增加“status=3 返回 422;创建后 dashboard total=桶合计”。

---

## BUG-BE-02 【L·后端】priority 无枚举校验,任意字符串入库

- **现象**:`POST /api/pool`(及 story)可提交 `priority:"Wont"` 等任意值并成功 200 入库,而前端只允许 Must/Should/Could,MoSCoW 口径会被破坏。
- **根因**:Pydantic 仅 `str` 类型,无 `Literal["Must","Should","Could"]`。
- **修复建议**:`priority: Literal["Must","Should","Could"] = "Must"`(schemas 统一),返回 422 拒绝。
- **回归用例**:新增 422 断言。

---

## BUG-BE-03 【M·后端】owner_id 引用不存在用户 → HTTP 500

- **现象**:`POST /api/stories {owner_id:999}` 返回 **500 Internal Server Error**(实测),应返回 4xx 业务错误。
- **根因**:`owner_id` FK 指向 users;无效引用未预检,SQLAlchemy 抛 IntegrityError 未被捕获,由 FastAPI 默认 500 兜底。
- **修复建议**:创建/更新故事前校验 owner 存在(不存在返回 400/404 detail),或捕获 IntegrityError 转 409/400;日志保留 traceback。
- **回归用例**:`POST owner_id=999 → 400/404;库内无残留行`。

---

## 观察项(非阻断,建议文档化)

| 编号 | 说明 |
|---|---|
| OBS-BE-01 | `GET /api/stories?q=` 只匹配 id/title;前端本地筛选还包含 description/acceptance/负责人/活动。两种口径宜在文档注明,避免验收争议。 |
| OBS-BE-02 | 删除故事后 `story_logs` 历史保留(story_id 无 FK),符合“变更留痕”,建议在 README 说明日志为软引用。 |
| OBS-BE-03 | 需求池/AI 建议在后端阶段 1 无种子与写接口(前端在线模式下载空池);符合阶段 1 范围,但演示时应说明“需求池与 AI 建议仅前端演示数据”。 |

---

## 2026-09-07 全面审查 · 新增缺陷

## BUG-FE-02 【M·前端】像素风演示副本数据漂移,缺 M21–M23

- **现象**:`爱管理_像素风用户故事看板.html`(独立演示页)内置静态 seed 仅 M01–M20,主应用基线已是 23 条(M21–M23 为看板↔甘特血缘重构新增);footer 文案「已同步 20 条故事(M01–M20)」、新故事 ID 保底 `Math.max(20,...)` 同步过期。若用该页演示,展示的是过时数据。
- **根因**:血缘重构只更新了主应用 `index.html` 与后端,未同步该演示副本。
- **修复**:seed 补齐 M21–M23 三条(格式与权威源逐字段一致);footer/恢复演示提示/ID 保底值三处 20→23。已复核:23 条连续无缺失、无「20 条」残留、标签数不变。
- **状态:已修复(2026-09-07),复核通过。**

## BUG-FE-03 【L·前端】离线模式残留口径过期(3 处)

- **现象**:
  1. `index.html` 离线新建故事 ID 保底 `Math.max(20,...)`(约 L1374),stories 为空的边缘情况下会生成 M21,与基线语义冲突;
  2. MILESTONES M4 文案「全 24 条回归」口径含混——需求基线是 US01–US24(24 条),看板故事卡基线是 M01–M23(23 张),两个数字并存易误解;
  3. footer 占位「已同步 20 条故事」(L540,渲染前闪现,JS 动态覆盖)。
- **修复**:保底值 20→23;M4 文案改「US01–US24 需求全量回归」明确需求条目口径;占位 20→23。
- **状态:已修复(2026-09-07),复核通过(无 `Math.max(20`/「全 24 条」/「已同步 20」残留)。**

## BUG-BE-04 【M·后端】viewer 只读角色无写操作拦截(M03 Must 验收缺口)

- **现象**:M03「创建角色与分配权限」为 Must 级,验收标准为「新建只读角色确实改不了东西」。但后端 `POST/PATCH /api/stories`、`PATCH /api/tasks`、`POST/DELETE/promote /api/pool` 仅校验登录,viewer 角色可直接写库;且无 viewer 演示账号,验收场景无法演示。
- **根因**:写端点统一用 `get_current_user`(仅认证),未做角色分级;seed 也只有 admin/owner/member×2 四类账号。
- **修复**:
  - 后端 6 个写端点改 `require_roles("admin","owner","member")`(stories.py×2、tasks.py×1、pool.py×3);agent.py/meetings.py 复查确认已有拦截;
  - seed 增加「成员5(viewer)」演示账号,前端登录页可一键填充;
  - 前端 `isViewer()`(仅在线+viewer 生效,不影响离线与其他角色)拦截 9 处写入口(新建/编辑弹窗、看板拖拽、需求池增删/promote/保存),`viewerDeny()` 统一提示;
  - 新增 `backend/tests/test_viewer_role.py` 7 条:viewer 写全 403 + 只读 GET 200 + member 对照组不拦截过宽。
- **状态:已修复(2026-09-07),复核通过(124 pytest 全绿,含 7 条新用例;120 E2E 全绿证明对 admin/owner/member 现有行为零影响)。**

## BUG-QA-01 【L·测试】meeting-improvements spec 清理缺口,QA 库残留临时卡

- **现象**:执行 `qa/meeting-improvements-e2e.spec.js` 后,QA 库残留其临时卡 M24(标题「交互回归-<时间戳>-仅改标题」),导致 `verify_migration.py --qa` 报「看板故事总数=23 :: 实际值=24」。2026-09-07 全量回归时实际命中,已手动清理(该卡无子任务挂载,孤儿=0,DELETE 安全)。
- **根因**:该 spec 的「修改后采纳」场景创建了新卡,但末尾没有像 ui-e2e(OE-C1)/kanban-gantt(OE-KGN-02c)那样补清理步骤——与「在线用例可逆」的项目约定不符。
- **修复建议**:spec 末尾追加清理(删除本次创建的临时卡并断言回到 23),或复用 ui-e2e 的清理模式;跑套件顺序上,若先跑本 spec 再跑 verify,预期 24 属已知行为。
- **状态:待修(非阻断;QA 库当前已清理回 23,dev 库不受影响)。**

## 2026-09-07 全面审查 · 待完善观察项(非阻断)

| 编号 | 严重级 | 说明 |
|---|---|---|
| OBS-BE-04 | M | 日志写入非原子:`_add_log` 内部自带 `db.commit()`,与主操作分属两个事务;create/patch/delete 均为“先提交业务、再提交日志”,日志写失败时业务已成事实,审计链断裂。建议并入主事务(去掉内部 commit,由调用方统一 commit)。 |
| OBS-BE-05 | L | `meetings.py` `_out()` 每条建议 3–4 次 `db.get`(meeting/agent_run/approval_payload),列表场景逐条放大成 N+1。当前数据量小无感知;建议列表查询用 join/批量预取。 |
| OBS-BE-06 | M | `requirements.txt` 全部 `>=` 仅 bcrypt 钉死,无 lock 文件;队友/评委复现环境可能拉到不兼容新版本。建议 `pip freeze` 固化或补 lock。 |
| OBS-BE-07 | L | 多处 `datetime.utcnow()`(Python 3.12+ DeprecationWarning,本次回归 884 条警告全来源于此):security.py/models.py/jobs.py/agent.py。建议统一迁移 `datetime.now(UTC)`。 |
| OBS-BE-08 | L | `q` 模糊查询未转义 `%`/`_`(用户搜 `%` 会当通配符,非注入但行为怪);列表接口无分页。数据量大后再做。 |
| OBS-SEC-01 | M | 安全配置项集合(课设可接受,答辩需知):①`config.py` 默认 DATABASE_URL 带真实密码、JWT_SECRET 弱默认值,.env 缺失时静默降级;②登录页一键填充密码 123456(与 M01“安全使用”叙事矛盾,演示便利 vs 验收口径);③CORS `*`;④JWT 无法吊销、token 存 localStorage。 |
| OBS-QA-02 | L | 测试基线数字未同步:qa/README.md(117/237)与 测试报告_汇总.md 仍为旧基线,当前实际 124 pytest + 120 E2E(BUG-BE-04 修复后)。下次更新测试文档时一并对齐。 |

---

## 修复复核记录(测试独立复核 · 2026-09-06)

> 复核方式:测试人员逐条核验修复代码 → 全量自动化回归 → 真实 HTTP/浏览器实测。未采纳开发口头结论。

### 复核一:修复代码核验(通过)

| 缺陷 | 修复位置 | 修复内容 |
|---|---|---|
| BUG-FE-01 | `index.html` bootstrap() L1375 | `/api/auth/me` 成功后补 `updateChip()` |
| BUG-BE-01 | `backend/app/schemas.py` | `status` ge0–le2、`sprint` ge1–le3、`activity` ge1–le5(StoryIn/StoryPatch) |
| BUG-BE-02 | `backend/app/schemas.py` | `priority: Literal["Must","Should","Could"]`(StoryIn/StoryPatch/PoolIn) |
| BUG-BE-03 | `backend/app/routers/stories.py` | create/patch 前校验 owner 存在,不存在返回 400「负责人不存在」 |

### 复核二:后端全量自动化回归

```
D:\aiguanli-venv\Scripts\python.exe -m pytest tests -q
41 passed, 2 warnings in 12.20s
(原 11 + 测试人员扩展 23 + 缺陷回归 7 = 41)
```

新增回归载体:`backend/tests/test_regression_bugs.py`(7 条,覆盖 status/sprint/activity 越界、story/pool priority 枚举、owner 不存在、dashboard 口径)。

### 复核三:真实 HTTP 实测(修复后 QA 后端 8001/AIcap_qa)

| 探针 | 修复前 | 修复后实测 | 结论 |
|---|---|---|---|
| POST story {status:5} | 200 入库 | **422** | ✅ |
| POST story {sprint:99} / {activity:9} | 200 入库 | **422** | ✅ |
| POST story / pool {priority:"Wont"} | 200 入库 | **422** | ✅ |
| POST story {owner_id:999} | 500 | **400** | ✅ |
| PATCH story {status:5} | 200 | **422** | ✅ |
| 合法写入 + dashboard | total 21≠桶合计 20 | total=桶合计(**一致**) | ✅ |
| 开发库 8000 与 QA 8001 同探针 | — | 均 422(两实例均加载修复) | ✅ |

### 复核四:前端 E2E 回归(Edge headless + Playwright)

```
===== UI E2E 结果: 48 passed / 0 failed =====
OE-12 刷新后状态栏仍显示已连接(BUG-FE-01 回归)  ✔
```
离线 35/35 + 在线联调 12/12 + 清理 1/1;BUG-FE-01 场景(OE-12)已由红转绿,在线 CRUD/拖拽/越权/共享库一致全部保持通过。

### 复核结论

- BUG-FE-01 / BUG-BE-01 / BUG-BE-02 / BUG-BE-03:**全部修复,测试复核通过,可关闭。**
- 观察项 OBS-BE-01/02/03 维持“文档化处理”,不阻塞(开发侧已在评审意见中确认)。
- 遗留建议:修复尚未 commit(前端接入与修复均在工作区),由开发/负责人决定提交拆分;`qa/ui-e2e.spec.js` 中 OE-12 已更新为正向断言,回归用例可长期复用。

---

## 修复复核记录(测试独立复核 · 2026-09-07)

> 复核方式:与 2026-09-06 批次相同——逐条核验修复代码 → 全量自动化回归 → 数据库一致性校验,不采纳口头结论。
> 本轮来源:项目全面审查(数据口径 + 权限模型 + 工程质量)发现的 4 项缺陷与 7 项观察项,其中 BUG-FE-02/03、BUG-BE-04 当轮修复。

### 复核一:修复代码核验(通过)

| 缺陷 | 修复位置 | 修复内容 |
|---|---|---|
| BUG-FE-02 | `爱管理_像素风用户故事看板.html` | seed 补 M21–M23;footer/恢复提示/ID 保底 20→23 |
| BUG-FE-03 | `index.html` L540/L808/L1374 | hintcount 占位 20→23;M4 文案改「US01–US24 需求全量回归」;离线 ID 保底 20→23 |
| BUG-BE-04 | `stories.py`/`tasks.py`/`pool.py` + `seed.py` + `index.html` + 新测试 | 6 写端点 `require_roles(admin/owner/member)`;seed 增成员5(viewer);前端 `isViewer()` 拦 9 处写入口;`test_viewer_role.py` 7 条 |

### 复核二:后端全量自动化回归

```
D:\aiguanli-venv\Scripts\python.exe -m pytest tests -q
124 passed, 884 warnings in 44.12s
(原 117 + viewer 角色新增 7 = 124;warnings 为存量 utcnow 弃用,见 OBS-BE-07)
```

### 复核三:前端 E2E 全量回归(QA 库 8001,五套件)

```
ui-e2e.spec.js               48 passed / 0 failed
kanban-gantt-e2e.spec.js     36 passed / 0 failed
meeting-review-e2e.spec.js   12 passed
meeting-improvements-e2e.spec 11 passed
meeting-agent-e2e.spec.js    13 passed
合计 120 项全绿;admin/owner/member 现有行为零回归(viewer 拦截条件严格限定)
```

### 复核四:数据库一致性校验(verify_migration.py,只读)

```
dev 库(8000):  6/6 通过(23 卡/0 孤儿/9 挂载/12 feature 子任务)
QA 库(8001):   复核中命中 BUG-QA-01(meeting-improvements 残留 M24 → 24 卡),
               手动清理后回到 23 卡;该卡无子任务,DELETE 安全
```

### 复核结论

- BUG-FE-02 / BUG-FE-03 / BUG-BE-04:**修复复核通过,可关闭。**
- BUG-QA-01(meeting-improvements spec 清理缺口):**待修**,非阻断;再次执行该 spec 前需知晓 QA 库将出现 M24 残留。
- 观察项 OBS-BE-04~08 / OBS-SEC-01 / OBS-QA-02:待完善,按需排期(优先级建议:OBS-BE-04 日志原子性 > OBS-BE-06 依赖锁定 > 其余)。
- M03 验收缺口已补齐:viewer(成员5)登录后,后端 6 写端点全部 403、只读接口 200、前端 9 写入口统一提示,验收标准「新建只读角色确实改不了东西」可直接演示。
