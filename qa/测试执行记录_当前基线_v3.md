# 爱管理 · 自动化测试执行记录(当前基线 v3)

- 执行日期:2026-09-13
- 被测对象:分支 `feat/javaweb-backend` 工作副本(基线提交 **`12ec35b`** + 本轮缺陷修复)
  - 后端 `java-backend/`(Spring Boot 3.5.3 + MyBatis-Plus,JDK 23,8080)
  - 前端 `frontend/`(Vue3 + Vite + Pinia,dev 5173)
- 用例设计依据:`qa/测试用例设计_当前基线_v3.md`(**168 条**:后端契约 126 + 前端 E2E 35 + 验收 7)
- 本轮工作分两阶段:**① 新建/补齐自动化**(新增 46 条:后端 Agent 契约 14 + 前端当前基线 E2E 32);**② 修复 7 条已确认缺陷并回归**(新增 4 条后端用例、前端 E2E 收敛为 35 条)

## 一、环境与前置

| 项 | 值 |
|---|---|
| MySQL | Docker 容器 `aiguanli-mysql`,宿主端口 **3307**;开发库 `AIcap`,契约测试库 `aicap_java_test` |
| 后端实例 | `127.0.0.1:8080`,**以 `java -jar target/aicap-java-backend.jar` 启动,并在启动前把 `backend/.env` 里的 `JWT_SECRET` / `AICAP_LLM_*` 注入进程环境**(否则 `/api/agent/config` 会是 `configured=false`,且缺 `JWT_SECRET` 会导致 token 校验失败) |
| 前端实例 | Vite dev server `http://localhost:5173`(hash 路由) |
| JDK / Maven | `JAVA_HOME=C:\Program Files\Java\jdk-23`;`E:\apache-maven-3.9.9\bin\mvn.cmd` |
| Node / Playwright | Node 24.15.0;Playwright 1.63.0,浏览器用系统 Edge(`channel: msedge`),虚拟麦克风 |
| 契约测试隔离 | 每个测试类以 `@SpringBootTest(properties=...)` 指向 `aicap_java_test`,上下文启动时执行 `classpath:db/reset_test_data.sql`(TRUNCATE 全部业务表 + 重置自增),随后 `DataSeeder` 重新播种 US01–US37 / T01–T16 / 5 用户 / 5 画像 |

> ⚠️ 关键前置:本轮后端新增了 `DELETE /api/meetings/{id}`,**修改源码后必须重新 `mvn -o -B package -DskipTests` 并重启 8080 实例**(Windows 下 jar 被运行中的进程占用会导致打包失败),否则 E2E 仍会打到旧代码上——实测未重启时 `DELETE /api/meetings/{id}` 返回 **405**,重启后为 **404/200**。

## 二、阶段一:后端契约测试(全量回归)

命令:

```powershell
cd java-backend
$env:JAVA_HOME='C:\Program Files\Java\jdk-23'
E:\apache-maven-3.9.9\bin\mvn.cmd -o -B test
```

| 轮次 | 用例数 | 结果 | mvn 总耗时 |
|---|---|---|---|
| 第 1 次(新建 Agent 套件前) | 108 | Tests run: 108, Failures: 0, Errors: 0 | — |
| 第 2 次(+ Agent 契约 14 条) | **122** | Tests run: 122, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS | 36.7s |
| 第 3 次(稳定性复跑) | 122 | 同上 — BUILD SUCCESS | 43.4s |
| 第 4 次(+ 缺陷修复后 4 条用例) | **126** | Tests run: 126, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS | 37.2s |

修复后逐类结果(`java-backend/target/surefire-reports/*.txt`):

| 测试类 | 用例数 | 结果 | 耗时 |
|---|---|---|---|
| `AgentContractTest`(阶段一新增) | 14 | PASS | 19.54s |
| `AuthContractTest` | 10(+1) | PASS | 4.23s |
| `BaselineContractTest` | 7 | PASS | 0.33s |
| `MeetingAudioContractTest` | 9 | PASS | 2.79s |
| `MeetingSuggestionReviewContractTest` | 24(+3) | PASS | 4.02s |
| `MemberProfileContractTest` | 9 | PASS | 0.46s |
| `PoolContractTest` | 9 | PASS | 0.28s |
| `RoleContractTest` | 9 | PASS | 0.46s |
| `StoryContractTest` | 12 | PASS | 0.37s |
| `TaskContractTest` | 23 | PASS | 1.14s |
| **合计** | **126** | **Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS** | 37.2s |

证据:`java-backend/target/surefire-reports/`、`target/full-test-run.log`(122 轮)、`target/full-test-run-after-fixes.log`(126 轮)。

### 2.1 会议 Agent 契约测试(阶段一新增,14 条)

此前 Agent 的 5 个接口**零自动化覆盖**。本轮新增 `AgentModelFixture`(本地 OpenAI 兼容 fixture,`127.0.0.1:19377`,JDK 自带 `com.sun.net.httpserver`)+ `AgentContractTest`:在 `@BeforeAll` 把 `AgentProperties` 的 `api-key/base-url/model` 指向 fixture 并保持 `aicap.llm.agent-worker-enabled=true`,于是「HTTP 排队 → AgentWorker 后台线程消费 → 工具循环 → 严格校验 → 建议落库」整条链路**真实执行**,但零外网、零费用、结果确定。

fixture 6 个场景:`HAPPY` / `PROPOSAL_CONFLICT` / `INVALID_EVIDENCE` / `COVERAGE_FIRST` / `TOOL_NOT_ALLOWED` / `PROVIDER_ERROR`;断言通过轮询 `GET /api/agent-runs/{id}` 等待终态。

### 2.2 阶段二新增的后端用例(4 条)

| 用例 | 方法 | 验证点 |
|---|---|---|
| `AUTH-10` | `login_viewerDisplayNameAlias_mapsToMember5` | viewer 的显示名「只读查看者」与 `成员5` 两种叫法登录到同一账号(id/role/display_name 一致,别名 token 可 `me`);错密码两者都 401 |
| `MEET-06` | `meeting_delete_ok_cascadesEverything` | `DELETE /api/meetings/{id}` 200 后:会议 404、runs 404、建议队列空、音频 404、落盘 mp3 已消失;**已审批产生的 pool_items 不受影响** |
| `MEET-07` | `meeting_delete_notFound_404` | 删不存在的会议 → 404 |
| `MEET-08` | `meeting_delete_forbidden` | viewer/member 403(会议仍在)、无 token 401 |

独立查库取证(单跑该类后):`aicap_java_test` 的 meetings / records / suggestions / payloads / runs / events / audio / pool **全部为 0**,`target/test-audio` 无残留 → 级联与删盘确实生效。

### 2.3 执行过程中的失败与修正(诚实记录)

| # | 现象 | 原因 | 处理 |
|---|---|---|---|
| 1 | `agent03` 期望 500,实测 **503** | `ApiException.server()` 映射为 503(与 FastAPI 版不同) | 断言改为 503 并记录契约口径;前端对任意非 2xx 都取 `detail` 展示,无兼容问题 |
| 2 | 9 个既有契约类把 worker 开关写成 `aicap.agent-worker-enabled=false` | `AgentProperties` 前缀是 `aicap.llm`,少 `llm.` → 属性**从未绑定**,测试上下文里 worker 实际为开启 | 统一修正(缺陷 AGENT-D01);新用例显式写 `true`,保证只有 Agent 套件上下文有 worker 消费队列 |
| 3 | `mvn package` 在 3 秒内 BUILD FAILURE | 运行中的 8080 实例占用 `target/aicap-java-backend.jar`(Windows 文件锁) | 先停实例 → 再打包 → 再以 `backend/.env` 注入环境变量启动 |
| 4 | 修复后前端子任务实测 `DELETE /api/meetings/{id}` 仍 **405** | 8080 上跑的是修复前的旧 jar | 重启实例后复测:不存在的会议 **404**、真实删除 **200**、再删 **404** |

## 三、阶段一/二:前端 E2E(当前基线回归套件)

命令:

```powershell
cd qa
npm install            # 仅首次(aicap-qa 独立工程,@playwright/test 1.63.0;浏览器用系统 Edge,不下载)
npx playwright test    # = npm test
```

| 阶段 | 轮次 | 用例数 | 结果 | 耗时 |
|---|---|---|---|---|
| 阶段一 | 子任务 3 轮 + 我复跑 4 轮 | 32 | 每轮 32 passed / 0 failed / 0 flaky | 54.9 / 56.5 / 57.0 / 56.3s(我) |
| 阶段二(修复后,含改造用例) | 我复跑 | **35** | **35 passed / 0 failed** | 1.0m |

分模块(全部 PASS):FE-AUTH 4、FE-OVW 2、FE-POOL 4、FE-BRD 6、FE-GNT 5、FE-MBR 4、FE-UML 3、**FE-AI 6**、FE-OFF 1。

证据:`qa/report/index.html`、`qa/test-results`。

### 3.1 阶段二的用例改造

| 用例 | 改造 |
|---|---|
| `FE-AI-01` | 由"固定标题复用(回避无删除接口)"改回**真正自清理**:建会议 → 落库 → 选中 → 点 `#meeting-delete`(接受 confirm)→ toast「会议已删除」→ 再删 404 → 列表/下拉/建议队列均已清空 |
| `FE-AI-04` | 新增:admin 的删除入口可用;点击后**取消**确认框,会议必须仍在(不真删) |
| `FE-AI-05` | 新增:member 的删除入口 `disabled` 且 `title` 含「管理员或负责人」 |
| `FE-AI-06` | 新增:viewer 的删除入口 `disabled` 且 `title` 含「只读」 |
| `FE-AUTH-02` | 断言改为「保留后端原因」:`#toast` 含「登录失败：用户名或密码错误」,且**不含**「未登录」「登录已过期」 |
| `FE-AUTH-03` | 改为断言写入口 **disabled + title**;并新增「viewer 用显示名『只读查看者』登录 → 200」 |

## 四、验收套件(前端新功能验收)

```powershell
cd frontend
npm run e2e:verify
```

| 阶段 | 结果 | 耗时 |
|---|---|---|
| 阶段一 | 7 passed,exit 0 | 20.8s |
| 阶段二(缺陷修复后) | 7 passed,exit 0 | 20.0s / 20.6s |

`VRF-01..05` = `e2e-verify/mcc-port.spec.js`(基线与看板 / 甘特详情与任务编辑往返 / 成员任务图与抽屉 / UML 双图 / 离线回落);`VRF-06..07` = `e2e-verify/recorder-profile.spec.js`(成员画像编辑 / 录音:麦克风 → MP3 → 会议提交 → 回放)。

录音用例的真实证据(测试日志节选):

```
[recorder] clip=待提交录音 · 00:03 · 56 KB（已编码 MP3）
[recorder] uploaded row=录音-2026-09-13T02-53-15.mp3网页录音 · 56 KB · 00:03 · sha256 c9f7321231…
[recorder] playback ok src=blob:http://localhost:51...
[recorder] delete ok
[recorder] meeting cleaned up: 1650c969-bf7e-46e4-afb9-a54afb1fb507
```

最后一行是阶段二新增的**会议自清理**:该 spec 原先每跑一轮都在开发库留一条「录音验收会议 <时间戳>」,现已改为跑完即调 `DELETE /api/meetings/{id}` 删除,实测复跑后会议总数不变。

## 五、环境副作用核查

执行全部三套件后,对开发库 `AIcap` 逐项核对(2026-09-13):

| 项 | 期望基线 | 实测 | 结论 |
|---|---|---|---|
| 故事 | 37(US01..US37,无 QA 残留) | 37 | ✅ |
| 任务 | 16;T16 还原为 `8h / W1–W6 / owner1 / story_ref US07,US24,US37` | 16;T16 = 8 / 1 / 6 / 1 / 上述引用 | ✅ |
| 用户 / 成员画像 | 5 / 5 | 5 / 5 | ✅ |
| 会议音频 | 0(用例上传后即删除) | 0 | ✅ |
| 需求池 | 执行前后一致 | 2 → 2(历史冒烟残留,ENV-D01 未处理) | ✅ 未被测试改动 |
| 会议 | 不增长 | 4 → 10(阶段一 E2E 残留 6 条)→ 5(阶段二清理 QA 残留 3 条 + 录音验收残留 3 条) | ✅ 阶段二后零增长 |
| 契约测试库 | 独立于开发库 | 使用 `aicap_java_test`,启动即 TRUNCATE + 重播种 | ✅ 开发库零影响 |

> 测试过程中发现并清理的数据残留:①阶段一 `FE-AI-01` 每次运行新增的 `QA-AI-MEETING-*`(共 6 条,已 SQL 清理,删除前确认无 audio/records/runs 关联);②验收套件 `recorder-profile.spec.js` 每轮新增的「录音验收会议 <时间戳>」(共 3 条,已按 id 清理)。两处**根因均已修**(前者改为真删除自清理,后者新增 DELETE 调用),后续运行不再累积。

## 六、一键编排与基础设施修复

最终一键编排(`& .\qa\run-all.ps1`,含服务在线预检):

```
backend-contract (JUnit, 126 cases)      PASS  Tests run: 126, Failures: 0, Errors: 0, Skipped: 0
frontend-e2e (qa/, 35 cases)             PASS  35 passed (1.0m)
acceptance (e2e-verify, 7 cases)         PASS  7 passed (20.2s)
suites: 3, failed: 0 → ALL SUITES PASSED (exit 0)
```

编排过程中暴露并修掉的两个**脚本级**问题:

| 编号 | 现象 | 原因 | 修法 |
|---|---|---|---|
| INFRA-D01 | `run-all.ps1` 的后端步骤 `Tests run: 0` + `BUILD FAILURE`,报 `class file version 67.0 ... only recognizes class file versions up to 52.0` | 脚本只在 `JAVA_HOME` 未设置时才指向 JDK 23,而本机环境里 `JAVA_HOME=D:\jdk8u312`(JDK 8)→ surefire 用 Java 8 fork | 改为**强制**指向 JDK 23(校验 `bin\java.exe` 存在,`-JavaHome` 可覆盖),并打印实际使用的 `JAVA_HOME` |
| INFRA-D02 | 给脚本加中文注释后 `run-all.ps1` 直接语法报错(`Unexpected token '}'`) | Windows PowerShell 按 GBK 解析 `.ps1`,非 ASCII 注释字节破坏语法 | 恢复 **ASCII-only**(脚本顶部写明约束),并用 `Parser::ParseFile` 自检通过 |

## 七、一次偶发失败的定位(诚实记录)

| 项 | 内容 |
|---|---|
| 现象 | 某次编排运行中 `FE-BRD-05`(按负责人筛选的精确卡片数)与 `FE-GNT-04`(甘特父卡详情)同时失败,其余 33 条通过 |
| 排查 | 两条用例单独复跑**通过**;随后连续 **3 轮**完整套件均 35/35;编排运行 35/35 |
| 结论 | **并发干扰**,非产品缺陷:失败时刻另有一个进程(文档核对)在同一套 8080/5173 与同一开发库上跑单条 Playwright 用例,而这两条用例含"精确条数/固定故事标题"断言 |
| 固化 | `qa/README.md` 写入**独占约束**:套件假设独占开发库与两个服务,勿与其他 E2E 进程或手工操作并发执行 |

## 八、最终环境副作用核查(编排运行之后)

stories 37 / tasks 16(T16 = 8h,W1–W6,owner1)/ users 5 / profiles 5 / meeting_audio 0 / 需求池 2(执行前后一致)/ 会议 5 且 `QA-AI-*` 为 **0** —— 全部符合基线,零增长。
