# 上手指南 · 克隆仓库后怎么跑起来

> 适用:4 人团队拿到本仓库后的首次运行。前端 + 后端 + MySQL 全在本机跑通。

## 0. 需要先装三样东西(一次性)

| 软件 | 用途 | 去哪装 |
|---|---|---|
| **Git** | 拉取/推送代码 | https://git-scm.com |
| **Docker Desktop** | 跑 MySQL 数据库 | https://www.docker.com/products/docker-desktop/ |
| **Python 3.11+** | 跑后端 FastAPI | https://www.python.org/downloads/ |

> 中国大陆网络提示:如果 Docker 拉取 `mysql:8.0` 镜像卡住/超时,说明直连 Docker Hub 不通 —— 需要**开 VPN**,或在 Docker Desktop → Settings → Docker Engine 里配置国内镜像源(registry-mirrors)后重启 Docker。

---

## 1. 拉取代码

在你电脑选个文件夹,执行:

```bash
git clone https://github.com/Wayne-SQS/AIcap.git
cd AIcap
```

> 想提交(push)改动需要仓库所有者到 **Settings → Collaborators** 把你加为协作者;只拉取不用。

## 2. 启动数据库(MySQL,走 Docker)

```bash
cd backend
docker compose up -d
```

- 首次会自动拉取 `mysql:8.0` 镜像并初始化,等 30–60 秒。
- 验证:`docker ps` 应看到 `aiguanli-mysql` 状态 Up。
- 数据库名 **AIcap**,账号 `aiguanli` / 密码 `aiguanli-2026`,端口 **3307**(避开系统自带 MySQL 的 3306)。

## 3. 配置后端环境(可选,但有密钥最好做)

复制模板并改密钥(模板里的连接串已能直接用):

```bash
copy .env.example .env
```

然后编辑 `backend/.env`,把 `JWT_SECRET=` 换成一段随机长字符串(随意打一串字母数字即可)。

## 4. 安装后端依赖(一次性)

```bash
python -m venv .venv
.venv\Scripts\activate        # Windows;Mac/Linux 用 source .venv/bin/activate
pip install -r requirements.txt
```

## 5. 启动后端

```bash
cd backend          # 若已在 backend 目录则跳过
.venv\Scripts\python.exe -m uvicorn app.main:app --reload --port 8000
```

看到 `Uvicorn running on http://127.0.0.1:8000` 即成功。首次启动会自动建表并播种(4 成员 / 20 故事 / 16 任务)。

> 验证接口:浏览器打开 http://127.0.0.1:8000/docs

## 6. 打开前端

双击仓库根目录的 **`index.html`**(或部署到 GitHub Pages 的线上地址)。

- 后端在跑 → 弹出**像素风登录框**(4 个账号,密码都是 `123456`),登录后是真多人协作数据;
- 后端没开/打不开 → 自动进入**离线演示模式**(右上角橙色「离线 · 演示数据」),纯本地演示。

| 账号 | 角色 | 权限 |
|---|---|---|
| 成员1 | 管理员(admin) | 全部,含删除 |
| 成员2 | 负责人(owner) | 全部,含删除 |
| 成员3 / 成员4 | 成员(member) | 读写,不能删除故事 |

## 7. 日常协作(git 三板斧)

```bash
git pull            # 先拉队友改动(每次写代码前必做)
# ...改文件...
git add . && git commit -m "谁: 改了啥"
git push
```

**冲突怎么办**:你和队友改了同一行才会冲突。`git pull` 报冲突时,打开冲突文件,保留要的部分、删掉 `<<<<<<< / ======= / >>>>>>>` 标记,再 `git add` + `commit` + `push`。

## 8. 想 4 人共享同一份数据?

- 每人在自己电脑跑 = 各自一套库,数据不互通(各自开发/演示够用)。
- 要共享:让其中一人当"服务器"(跑第 2/5 步),把 `index.html` 顶部 `API_BASE` 从 `http://127.0.0.1:8000` 改成他的局域网 IP(如 `http://192.168.x.x:8000`),其余人打开同一个前端就连他那份数据。
- 以后上线可租一台云服务器,把第 2/5 步跑在服务器上。

## 常见问题

| 现象 | 解决 |
|---|---|
| `docker compose up` 拉镜像卡住 | 开 VPN 或配国内镜像源(见第 0 节) |
| 端口 3307 被占用 | 改 `backend/docker-compose.yml` 里的 `3307:3306` 为别的端口,并同步改 `.env` 连接串 |
| 登录报「用户名或密码错误」 | 确认后端已启动且 `/api/health` 返回 ok;密码是 123456 |
| 双击 index.html 白屏 | 用 Chrome/Edge 打开;或本地起静态服务 `python -m http.server 8080` |
