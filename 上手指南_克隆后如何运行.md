# 上手指南 · 克隆仓库后怎么跑起来

> 适用:4 人团队拿到本仓库后的首次运行。前端 + 后端 + MySQL 全在本机跑通。

## 0. 需要先装这几样东西(一次性)

| 软件 | 用途 | 去哪装 |
|---|---|---|
| **Git** | 拉取/推送代码 | https://git-scm.com |
| **Docker Desktop** | 跑 MySQL 数据库 | https://www.docker.com/products/docker-desktop/ |
| **JDK 23** | 跑 JavaWeb 后端(`JAVA_HOME` 须指向它,别指到旧 JDK) | https://www.oracle.com/java/technologies/downloads/ |
| **Maven 3.6.3+** | 构建/启动后端 | https://maven.apache.org/download.cgi |
| **Node.js 18+** | 构建/开发前端(含 npm) | https://nodejs.org/ |

> 中国大陆网络提示:如果 Docker 拉取 `mysql:8.0` 镜像卡住/超时,说明直连 Docker Hub 不通 —— 需要**开 VPN**,或在 Docker Desktop → Settings → Docker Engine 里配置国内镜像源(registry-mirrors)后重启 Docker。

---

## 1. 拉取代码

在你电脑选个文件夹,执行:

```bash
git clone https://github.com/Wayne-SQS/AIcap.git
cd AIcap
```

> 想提交(push)改动需要仓库所有者到 **Settings → Collaborators** 把你加为协作者;只拉取不用。

## 2. 启动数据库(MySQL,走 Docker)

```bash
cd backend
docker compose up -d
```

> `backend/` 是 FastAPI 旧后端的归档目录,但其中的 `docker-compose.yml` 仍是启动 MySQL 的现行方式。

- 首次会自动拉取 `mysql:8.0` 镜像并初始化,等 30–60 秒。
- 验证:`docker ps` 应看到 `aiguanli-mysql` 状态 Up。
- 数据库名 **AIcap**,账号 `aiguanli` / 密码 `aiguanli-2026`,端口 **3307**(避开系统自带 MySQL 的 3306)。

## 3. 配置后端环境(可选)

现行 JavaWeb 后端默认连 `127.0.0.1:3307` 的 `AIcap` 库(账号/密码见上),开箱即用,无需配置文件。如需覆盖,用环境变量:`DB_URL`、`DB_USER`、`DB_PASSWORD`、`JWT_SECRET`、`SERVER_PORT`(全部可选项见 `java-backend/src/main/resources/application.yml`;`JWT_SECRET` 默认有开发值,生产建议覆盖)。

## 4. 安装后端依赖(一次性)

Maven 会在首次构建时自动下载依赖,无需手动安装:

```bash
cd java-backend
mvn package -DskipTests    # 首次较慢(下载依赖),成功后 target/ 出现 aicap-java-backend.jar
```

## 5. 启动后端(JavaWeb,默认 8080)

```bash
cd java-backend            # 若已在 java-backend 目录则跳过
mvn spring-boot:run
# 或者用第 4 步打好的 jar:java -jar target\aicap-java-backend.jar
```

看到 `Started AicapJavaBackendApplication`(Tomcat started on port 8080)即成功。

> 验证接口:浏览器打开 http://127.0.0.1:8080/api/health ,应返回 `{"status":"ok","db":true}`。

> 注意:Java 后端**启动时自动建表并播种**,已不再依赖 FastAPI:首次启动会执行 `java-backend/src/main/resources/db/schema.sql`(全部 `CREATE TABLE IF NOT EXISTS`,幂等),并在对应表为空时写入 5 个演示用户(密码 123456)/ 23 条故事 / 16 条任务(对齐原 FastAPI 种子;可用 `AICAP_SEED_ON_START=false` 关闭播种)。全新空库直接启动即可;已有数据的库启动时自动跳过,不会覆盖。

## 6. 打开前端(Vue3,默认连 8080)

```bash
cd frontend
npm install        # 首次
npm run dev        # 开发模式,浏览器打开 http://localhost:5173
```

前端默认把 API 指向 `http://127.0.0.1:8080`(现行 JavaWeb 后端)。

- 后端在跑 → 弹出**像素风登录框**(5 个账号,密码都是 `123456`),登录后是真多人协作数据;
- 后端没开/打不开 → 自动进入**离线演示模式**(右上角橙色「离线 · 演示数据」),纯本地演示。

两个新功能(都需要先在线登录,离线模式下不生效):

| 功能 | 位置 | 说明 |
|---|---|---|
| **成员画像** | 侧栏「成员任务图」页顶部 | 技术栈 / 工作能力 / 熟悉的开发流程领域(带熟练度)。admin/owner 可改任何人,member 只能改本人,viewer 只读 |
| **会议录音** | 侧栏「AI 助手」→ 会议智能体 | 点「开始录音」调麦克风,停止后浏览器内编码为 **.mp3** 并提交到所选会议;也可选本地 .mp3。音频落盘在 `java-backend/data/audio/`(已 gitignore) |

> 录音依赖浏览器麦克风权限与 `lamejs`(随 `npm install` 安装)。Chrome/Edge 请允许本页访问麦克风;若提示不支持,仍可用「选择本地 .mp3 提交」。

本轮还移植了队友 mcc 在静态原型新版(`legacy/index.html`)里的内容:**US01–US37 需求基线(含 Sprint 4+ 后续路线)**、甘特图的 Sprint 分带 + 点击任务条详情 + 任务编辑弹窗、成员卡片点击开任务明细抽屉、周负载图例、成员开发活动图(6 周×7 天)、UML 真实 SVG 图(时序图按 Spring Boot 真实路由)。看板与总览的**工时加权血缘口径、卡面子任务徽章、「未分配」负责人**按原样保留,详见 README「静态原型新版内容移植」。

### 前端验收脚本(可选)

```bash
cd frontend
npm run e2e:verify   # 用系统 Edge + 虚拟麦克风跑 7 项:基线与看板血缘 / 甘特详情与任务编辑往返 / 成员抽屉 / UML / 离线回落 / 成员画像 / 录音提交回放
```

> 前置:后端 8080 与 `npm run dev`(5173)都已启动;无需下载 Playwright 浏览器(走系统 Edge)。

| 账号 | 角色 | 权限 |
|---|---|---|
| 李锐铭(旧别名 成员1) | 管理员(admin) | 全部,含删除 |
| 高思晗(旧别名 成员2) | 负责人(owner) | 全部,含删除 |
| 孙秋实 / 罗子涵(旧别名 成员3 / 成员4) | 成员(member) | 读写,不能删除故事 |
| 成员5「只读查看者」 | 观察者(viewer) | 只读 |

## 7. 日常协作(git 三板斧)

```bash
git pull            # 先拉队友改动(每次写代码前必做)
# ...改文件...
git add . && git commit -m "谁: 改了啥"
git push
```

**冲突怎么办**:你和队友改了同一行才会冲突。`git pull` 报冲突时,打开冲突文件,保留要的部分、删掉 `<<<<<<< / ======= / >>>>>>>` 标记,再 `git add` + `commit` + `push`。

## 8. 想 4 人共享同一份数据?

- 每人在自己电脑跑 = 各自一套库,数据不互通(各自开发/演示够用)。
- 要共享:让其中一人当"服务器"(跑第 2/5 步),其余人把前端 API 地址指向他的局域网 IP(如 `http://192.168.x.x:8080`):开发模式设环境变量 `VITE_API_BASE` 后再 `npm run dev`,或改 `frontend/src/api/client.js` 里的默认值。
- 以后上线可租一台云服务器,把第 2/5 步跑在服务器上。

## 常见问题

| 现象 | 解决 |
|---|---|
| `docker compose up` 拉镜像卡住 | 开 VPN 或配国内镜像源(见第 0 节) |
| 端口 3307 被占用 | 改 `backend/docker-compose.yml` 里的 `3307:3306` 为别的端口,并同步改后端 `DB_URL` 环境变量里的端口 |
| 登录报「用户名或密码错误」 | 确认后端已启动且 `http://127.0.0.1:8080/api/health` 返回 ok;密码是 123456 |
| 前端页面打不开/白屏 | 用 Chrome/Edge 打开 http://localhost:5173 ;`npm run build` 产物可用任意静态服务器伺服 `frontend/dist`(如 `npx http-server dist -p 8092`,别占用后端的 8080) |
| mvn 报 `UnsupportedClassVersionError` / class file version 61.0 | `JAVA_HOME` 指到了旧 JDK,改成 JDK 23 后重试 |
